export const IMAGE_SIZE = {
  SM_PX: 384,
  MD_PX: 640,
  MAX_PX: 2000,
};

export const IMAGE_SIZE_TAB = [
  IMAGE_SIZE.SM_PX,
  IMAGE_SIZE.MD_PX,
  IMAGE_SIZE.MAX_PX,
];

export const DEVICE = {
  SMALL: 768,
  MEDIUM: 1200,
};
